import { Lato, Montserrat, Lusitana } from 'next/font/google';

export const lato = Lato({ subsets: ['latin'], weight: '400' });

export const montserrat = Montserrat({ subsets: ['vietnamese'], weight: '400' });

export const lusitana = Lusitana({ subsets: ['latin'], weight: '400' });
